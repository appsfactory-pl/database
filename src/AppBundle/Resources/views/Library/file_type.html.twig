{% extends "::base.html.twig" %}

{% block title %}File Types List{% endblock %}
{% block header %}File Types List{% endblock %}

{% block body %}
    {% if types is not empty %}
        <div class="widget-box">
            <div class="widget-title"> <span class="icon"><i class="icon-th"></i></span>
                <h5>Data table</h5>
            </div>
            <div class="widget-content nopadding">
                <table class="table table-bordered data-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for type in types %}
                            <tr>
                                <td>{{ type.id }}</td>
                                <td>{{ type.name }}</td>
                                <td>
                                    <a href="{{ path('app_library_filetypeedit',{'id':type.id}) }}" class="btn btn-primary btn-xs"><i class="icon icon-edit"></i></a>
                                    <a href="{{ path('file_type_delete',{'id':type.id}) }}" class="btn btn-danger confirm"  name="library entry"><i class="icon icon-remove"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    {% endif %}
{% endblock %}
